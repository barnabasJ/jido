<!--
This file was generated by Spark. Do not edit it by hand.
-->
# Jido.Dsl.Agent

Spark DSL extension for `Jido.Agent`.

Defines four host-owned sections:

  * `agent do … end` — agent identity (name, description, path, schema)
  * `slices do … end` — slice/plugin mounts at agent-declared paths
  * `signal_routes do … end` — `route` entries
  * `schedules do … end` — `schedule` entries

The `slices do … end` section is the single source of truth for
path-to-slice binding on the agent. Each `slice :path, Module` line
mounts a slice (or plugin) at the agent-declared `:path`. Slice and
plugin modules no longer declare `path` on themselves — the agent
owns the binding so the same slice module can mount at different
paths on different agents.

Middleware lives at the top level on `use Jido.Agent, middleware: […]`
because order matters and a flat ordered list is the right shape.
Plugin modules with middleware behaviour appear in **both** places:
in `slices do …` for path/options, and in `middleware: […]` for
ordering.

The `extensions: […]` keyword on `use Jido.Agent` is reserved for
modules that contribute a typed DSL section to the host agent (the
`Jido.Slice.Extension` host-section mechanism, e.g. `react do … end`
for `Jido.AI.ReAct`). It is **not** the channel for slice/plugin
enumeration anymore.


## agent
Agent identity and base configuration.






### Options

| Name | Type | Default | Docs |
|------|------|---------|------|
| [`name`](#agent-name){: #agent-name .spark-required} | `String.t` |  | Agent name (letters, numbers, underscores). |
| [`description`](#agent-description){: #agent-description } | `String.t` |  |  |
| [`category`](#agent-category){: #agent-category } | `String.t` |  |  |
| [`tags`](#agent-tags){: #agent-tags } | `list(String.t)` | `[]` |  |
| [`vsn`](#agent-vsn){: #agent-vsn } | `String.t` |  |  |
| [`path`](#agent-path){: #agent-path } | `atom` |  | Atom slice key where the agent's user-domain state lives. Required when `schema:` is set; omit both for a pure composition agent. |
| [`schema`](#agent-schema){: #agent-schema } | `any` | `[]` | Zoi or NimbleOptions schema for the agent's slice state. Required when `path:` is set; omit both for a pure composition agent. |






## slices
Slice and plugin mounts. Each `slice :path, Module` registers a slice/plugin at the agent-declared path. The same module may be mounted at multiple paths (`slice :slack_support, SlackPlugin; slice :slack_sales, SlackPlugin`); at signal-route dispatch the framework fans the action out, calling it once per matching mount with each mount's own slice state and `ctx.slice_config` (per-mount config visible to the action).

### Nested DSLs
 * [slice](#slices-slice)





### slices.slice
```elixir
slice path, module
```


Mounts a slice or plugin at an agent-declared path.





### Arguments

| Name | Type | Default | Docs |
|------|------|---------|------|
| [`path`](#slices-slice-path){: #slices-slice-path .spark-required} | `atom` |  | Atom slice key in agent.state where this slice's value lives. |
| [`module`](#slices-slice-module){: #slices-slice-module .spark-required} | `atom` |  | Slice (`use Jido.Slice`) or plugin (`use Jido.Plugin`) module to mount. |
### Options

| Name | Type | Default | Docs |
|------|------|---------|------|
| [`options`](#slices-slice-options){: #slices-slice-options } | `keyword \| map` | `[]` | Configuration map / keyword list to seed the slice with. Merged with values from any contributed-section block (e.g. `react do … end`). |









## signal_routes
Compile-time signal route table.

### Nested DSLs
 * [route](#signal_routes-route)





### signal_routes.route
```elixir
route type, action
```


Maps a signal type/pattern to an action target.





### Arguments

| Name | Type | Default | Docs |
|------|------|---------|------|
| [`type`](#signal_routes-route-type){: #signal_routes-route-type .spark-required} | `String.t` |  |  |
| [`action`](#signal_routes-route-action){: #signal_routes-route-action .spark-required} | `atom \| mfa` |  |  |
### Options

| Name | Type | Default | Docs |
|------|------|---------|------|
| [`priority`](#signal_routes-route-priority){: #signal_routes-route-priority } | `integer` | `0` |  |
| [`match`](#signal_routes-route-match){: #signal_routes-route-match } | `(any -> any)` |  |  |
| [`static`](#signal_routes-route-static){: #signal_routes-route-static } | `map` |  |  |









## schedules
Declarative agent-level cron schedules.

### Nested DSLs
 * [schedule](#schedules-schedule)





### schedules.schedule
```elixir
schedule cron, signal_type
```


Declarative cron schedule.





### Arguments

| Name | Type | Default | Docs |
|------|------|---------|------|
| [`cron`](#schedules-schedule-cron){: #schedules-schedule-cron .spark-required} | `String.t` |  |  |
| [`signal_type`](#schedules-schedule-signal_type){: #schedules-schedule-signal_type .spark-required} | `String.t` |  |  |
### Options

| Name | Type | Default | Docs |
|------|------|---------|------|
| [`data`](#schedules-schedule-data){: #schedules-schedule-data } | `map` | `%{}` |  |
| [`job_id`](#schedules-schedule-job_id){: #schedules-schedule-job_id } | `any` |  |  |
| [`timezone`](#schedules-schedule-timezone){: #schedules-schedule-timezone } | `String.t` |  |  |










<style type="text/css">.spark-required::after { content: "*"; color: red !important; }</style>
